# sdd-archive SKILL

Purpose: finalize the change, consolidate artifacts, and produce an archive report for handoff.

Responsibilities:
- Collate proposal, spec, design, tasks, apply-progress, and verify-report.
- Produce an archive summary with links, decisions, rollout steps, and rollback plan.
- Save as `sdd/{change}/archive-report` via `mem_save`.
